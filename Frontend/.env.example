# Frontend environment variables
# NOTE: With vercel.json proxy setup, you do NOT need VITE_API_URL
# All /api/* calls are automatically forwarded to the backend via Vercel rewrites.

# Only needed if you want to connect directly without Vercel proxy:
# VITE_API_URL=https://habitquest-backend.onrender.com
